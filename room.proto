syntax = "proto3";

// All protocol buffers related to the room.

message RoomEvent {
    enum Type {
        JOINED = 0;
        LEFT = 1;
    }

    Type type = 1;
    string from = 2;
}

message RoomCommand {
    enum Type {
        EXIT = 0;
    }
    
    Type type = 1;
    bytes data = 2;
}
